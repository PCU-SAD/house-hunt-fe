import { Container } from '@/components'
import { FC } from 'react'

type HomePageProps = {}

const HomePage: FC<HomePageProps> = () => {
  return <Container className="mt-6">Home page</Container>
}

export default HomePage
